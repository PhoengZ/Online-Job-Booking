# Online job booking 
## Installation
1. install Brevo api by 
```bash
npm install @getbrevo/brevo
```
2. get api key from brevo website
3. add your api key to config.env 
4. install frontend nextjs with
```bash
npx create-next-app@latest frontend --yes
```